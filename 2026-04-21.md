  Module 7 — Infrastructure as Code (IaC)

  1. 为什么 AI Infra 必须 IaC

  场景：公司从 50 GPU → 5000 GPU，跨 3 个 region，10 个 AWS 账号。

  没有 IaC 的痛点：
  - Console 手点 → 无法追溯谁改了什么
  - 环境不一致 → dev 能跑，prod 炸
  - 灾难恢复没法自动化 → 一个 region 挂了，手动重建要几天
  - 没有 code review → 一个误操作改坏 prod VPC

  IaC 核心价值：
  - 基础设施即代码 → Git 版本控制 + PR review
  - Reproducibility → terraform apply 一键重建整个环境
  - Drift detection → 发现手动改动
  - Policy as Code → 自动检查合规性

  ---
  2. CloudFormation

  AWS 原生 IaC 工具。

  核心概念：

  Template (YAML/JSON)
      ↓
  Stack (Template 实例化后的一组资源)
      ↓
  Change Set (预览变更，apply 前确认)

  特点：

  ┌──────────────────┬─────────────────────────────────────────────┐
  │       维度       │                    说明                     │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ 语言             │ YAML / JSON（声明式）                       │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ 资源覆盖         │ 最全，AWS 新服务第一时间支持                │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ State 管理       │ AWS 托管，无需自己维护 state file           │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ Drift Detection  │ 内置，aws cloudformation detect-stack-drift │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ Rollback         │ 默认失败自动回滚                            │
  ├──────────────────┼─────────────────────────────────────────────┤
  │ 多账号/多 region │ StackSets                                   │
  └──────────────────┴─────────────────────────────────────────────┘

  StackSets 典型用法：
  # 一个 template 部署到所有 Organizations 账号
  # 例如：统一部署 CloudTrail、GuardDuty、baseline IAM Roles

  局限：
  - YAML 太 verbose，复杂逻辑（循环、条件）很难写
  - 部署慢（比 Terraform 慢 2-3 倍）
  - 只限 AWS，不能管 GCP/K8s 资源

  ---
  3. AWS CDK (Cloud Development Kit)

  定位：用编程语言（TypeScript/Python/Go/Java）写 IaC，底层 synth 成 CloudFormation。

  三层 Construct：

  ┌──────────────┬──────────────────────────────────────┬───────────────────────────────────────┐
  │     层级     │                 说明                 │                 例子                  │
  ├──────────────┼──────────────────────────────────────┼───────────────────────────────────────┤
  │ L1 (CFN)     │ 直接映射 CloudFormation 资源，最底层 │ CfnBucket                             │
  ├──────────────┼──────────────────────────────────────┼───────────────────────────────────────┤
  │ L2 (Curated) │ AWS 封装的高级 API，有合理默认值     │ Bucket                                │
  ├──────────────┼──────────────────────────────────────┼───────────────────────────────────────┤
  │ L3 (Pattern) │ 业务级模式，一行代码起一套架构       │ ApplicationLoadBalancedFargateService │
  └──────────────┴──────────────────────────────────────┴───────────────────────────────────────┘

  代码示例：
  // CDK: 10 行起一个生产级 VPC
  const vpc = new ec2.Vpc(this, 'MyVpc', {
    maxAzs: 3,
    natGateways: 3,
    subnetConfiguration: [
      { name: 'Public', subnetType: SubnetType.PUBLIC },
      { name: 'Private', subnetType: SubnetType.PRIVATE_WITH_EGRESS },
    ],
  });
  对应 CloudFormation 要写 200+ 行 YAML。

  适合场景：
  - 团队已经写 TypeScript/Python
  - 需要复杂逻辑（循环、条件、共享模块）
  - 想要 IDE 自动补全和类型检查

  不足：
  - 只限 AWS（底层是 CloudFormation）
  - 学习曲线比 Terraform 陡（需要掌握 Construct 层级）
  - 生成的 CloudFormation 可能很大，调试困难

  ---
  4. Terraform

  定位：多云 IaC 事实标准，HCL 语法。

  核心概念：

  .tf files (HCL)
      ↓
  terraform plan  (预览)
      ↓
  terraform apply (执行)
      ↓
  terraform.tfstate (记录当前状态)

  State 管理（最关键）：

  # Remote state backend (必须!)
  terraform {
    backend "s3" {
      bucket         = "my-tf-state"
      key            = "prod/network.tfstate"
      region         = "us-east-1"
      encrypt        = true
      dynamodb_table = "tf-state-lock"   # 防止并发 apply
    }
  }

  - S3 存 state file（加密 + versioning）
  - DynamoDB 做 state lock（避免两人同时 apply 造成 state 损坏）
  - State 机密性：state file 里可能含明文 password/key，必须加密 + 严格 IAM

  模块化：
  terraform/
  ├── modules/
  │   ├── vpc/
  │   ├── eks/
  │   └── rds/
  ├── environments/
  │   ├── dev/
  │   │   └── main.tf (引用 modules/)
  │   ├── staging/
  │   └── prod/

  Workspace（轻量环境隔离）：
  terraform workspace new dev
  terraform workspace new prod
  # 不推荐用于生产环境隔离，生产应该用独立的 state file + 独立账号

  ---
  5. 三者横向对比

  ┌─────────────────┬──────────────────┬──────────────────────────┬──────────────────────────────┐
  │      维度       │  CloudFormation  │           CDK            │          Terraform           │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 语法            │ YAML/JSON 声明式 │ TypeScript/Python 编程式 │ HCL 声明式                   │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 支持云          │ AWS only         │ AWS only (底层 CFN)      │ 多云（AWS/GCP/Azure/K8s）    │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ State           │ AWS 托管         │ AWS 托管（CFN）          │ 自己维护（S3+DynamoDB）      │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 资源更新速度    │ 慢               │ 慢（=CFN）               │ 快                           │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 社区 module     │ 较少             │ Construct Hub            │ Terraform Registry（巨量）   │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ Drift Detection │ 内置             │ 内置（=CFN）             │ terraform plan 自动检测      │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ Rollback        │ 自动（失败回滚） │ 自动（=CFN）             │ 无自动回滚，需手动           │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 学习曲线        │ 中               │ 陡                       │ 平缓                         │
  ├─────────────────┼──────────────────┼──────────────────────────┼──────────────────────────────┤
  │ 适合团队        │ 纯 AWS + 小团队  │ 写代码的团队 + 复杂逻辑  │ 多云 / 需要模块化 / K8s 管理 │
  └─────────────────┴──────────────────┴──────────────────────────┴──────────────────────────────┘

  ---
  6. 选型决策树

  需要管理多云 (AWS + GCP + K8s)？
  ├── 是 → Terraform
  └── 否 → 团队习惯写代码？
            ├── 是 → CDK（类型安全，复杂逻辑好写）
            └── 否 → 纯 AWS 简单场景？
                      ├── 是 → CloudFormation
                      └── 否 → Terraform（多数情况的默认选择）

  经验结论：AI Infra 团队的主流选择是 Terraform + Helm：
  - Terraform 管云资源（VPC、EKS 集群、RDS、IAM）
  - Helm 管 K8s 应用（Karpenter、External Secrets Operator、监控栈）
  - 两者不重叠：Terraform 停在 EKS 集群创建，Helm 接管集群内的一切

  ---
  7. 生产级最佳实践

  7.1 State 分拆

  不要把所有东西放一个 state file。

  按生命周期 + blast radius 拆分：
  ├── network.tfstate       (VPC、Transit Gateway — 几乎不变)
  ├── security.tfstate      (IAM Roles、KMS — 低频变更)
  ├── eks-cluster.tfstate   (EKS 集群 — 月度变更)
  ├── eks-addons.tfstate    (Karpenter、ESO — 频繁变更)
  └── applications.tfstate  (业务资源 — 每日变更)

  好处： 一个 state 损坏不影响其他；apply 速度快；blast radius 可控。

  7.2 CI/CD 集成

  PR 创建 → GitHub Actions → terraform plan → 自动评论 PR
      ↓ Code review + approve
  Merge → GitHub Actions → terraform apply (via Atlantis/TF Cloud)
      ↓
  Slack 通知 apply 结果

  关键原则：
  - 禁止本地 terraform apply（除了 break-glass 场景）
  - CI 用专用 IAM Role（OIDC federation，无长期 credentials）
  - plan 结果贴到 PR，强制 review

  7.3 Policy as Code

  Terraform plan → OPA/Sentinel 策略检查 → 通过才 apply

  典型策略：
  - S3 bucket 必须加密
  - EC2 不允许使用 t2 系列（性能不稳定）
  - 所有资源必须有 team/env/project tag
  - 禁止创建 0.0.0.0/0 的 SG 入站规则

  工具选择：
  - OPA (Open Policy Agent) — 开源，通用
  - Sentinel — HashiCorp 商业，Terraform Cloud 集成
  - tfsec / checkov — 静态扫描，CI 跑

  7.4 Drift Detection

  问题： 有人在 Console 手动改了资源（紧急修 prod），Terraform 不知道。

  解法：
  - 定期 (每天/每周) 跑 terraform plan -detailed-exitcode
  - 退出码 2 = 有 drift，触发告警
  - 专人修复：要么 import 手动变更到代码，要么 apply 还原

  7.5 Module 版本化

  module "vpc" {
    source  = "git::https://github.com/company/tf-modules.git//vpc?ref=v1.2.3"
    # 不是 main 分支，是打了 tag 的版本
  }

  理由： main 分支改了会影响所有使用者；必须版本化，升级由使用者主动触发。

  ---
  8. 常见面试考点

  9. Terraform vs CloudFormation 选型 — 多云选 TF，纯 AWS 小团队选 CFN，复杂逻辑选 CDK
  10. State 管理 — S3 backend + DynamoDB lock 是必考；state 机密性（含明文）是追问点
  11. State 损坏恢复 — S3 versioning 回滚；terraform state rm + terraform import 重建
  12. Drift detection — 为什么需要、如何实现、如何修复
  13. Module 版本化 — 为什么不能用 main 分支
  14. Policy as Code — OPA/Sentinel/tfsec，防止写错配置进 prod
  15. CI/CD 集成 — Atlantis / Terraform Cloud / GitHub Actions + OIDC
  16. 多账号部署 — CloudFormation StackSets vs Terraform workspace+独立 state

  ---
  Module 7 Summary

  ┌────────────────┬──────────────────────────────────────────────────────────┐
  │     知识点     │                        一句话记忆                        │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ CloudFormation │ AWS 原生，YAML，State 托管，StackSets 做跨账号           │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ CDK            │ TypeScript/Python 写 IaC，底层 synth 成 CFN              │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ Terraform      │ 多云事实标准，S3+DynamoDB 管 state，生态最强             │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ State 管理     │ Remote backend + lock 必备，按 blast radius 拆 state     │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ 最佳实践       │ CI/CD + Policy as Code + Drift Detection + Module 版本化 │
  ├────────────────┼──────────────────────────────────────────────────────────┤
  │ 选型核心       │ 多云 → TF，纯 AWS 简单 → CFN，复杂逻辑 → CDK             │
  └────────────────┴──────────────────────────────────────────────────────────┘

  Question 1:

  Your company is about to build a new AI platform on AWS from scratch. The eng team currently has 5 engineers, all
  comfortable with Python. The platform will run on EKS with Aurora, S3, and ~20 microservices. In 1 year, the team
  expects to expand to GCP for TPU workloads.

  Which IaC tool do you choose, and why? What's your rollout plan for the first 3 months?

✻ Churned for 1m 40s

※ recap: We're in Module 7 (Infrastructure as Code) of the AWS Infrastructure interview prep. I gave the learning
  material and posed mock Question 1: IaC tool choice for a new AI platform (5 Python engineers, EKS, plans to add GCP
  in 1 year) plus a 3-month rollout plan. Waiting for your answer. (disable recaps in /config)

❯ 因为可能是多云，而且比较复杂，用Terreform作为IaC工具比较好，我会让team先熟悉terraform，特别是多环境，多账号管理。建立
  起CI/CD 的pipeline，建立起Policy as Code和Drift Detection的最佳实践

● 好的回答，方向完全正确！反馈：

  ✅ 答对的部分：
  - 多云考虑 → 选 Terraform — 完全正确，这是 1 年后要去 GCP 的决定性因素
  - 先熟悉 Terraform → 务实，避免团队边学边踩坑
  - 多环境/多账号管理 — 抓住了 state 拆分的关键
  - CI/CD + Policy as Code + Drift Detection — 三个最佳实践都点到了

  ⚠️ 可以补充的（让答案更完整）：

  1. 3 个月的 rollout 应该更有阶段性：

  Month 1 — 基础建设 (Foundation)
  ├── 建 Module 库（VPC / EKS / RDS / IAM 可复用模块）
  ├── 建 State 架构（S3 + DynamoDB lock，按 blast radius 拆 state）
  ├── 建 CI/CD pipeline (GitHub Actions + OIDC，禁本地 apply)
  └── 打通 dev 环境，小流量验证

  Month 2 — 推广 + 治理
  ├── Staging 环境落地
  ├── Policy as Code (tfsec / checkov 扫描)
  ├── Module 版本化（打 tag，不用 main）
  └── 团队 onboarding + 文档

  Month 3 — 生产 + 观测
  ├── Prod 环境上线（所有资源 IaC 化）
  ├── Drift detection 自动化（每日跑 plan）
  ├── 跨账号架构准备（为 1 年后 GCP 和多账号铺路）
  └── Incident response runbook（state 损坏如何恢复）

  2. 一个容易忽略但加分的点 — 为 GCP 铺路：
  - Module 结构设计时，云厂商相关的 resource 封装在 provider-specific module 里（如 modules/aws-vpc/ vs
  modules/gcp-vpc/），上层业务 module 只依赖抽象接口
  - 这样 1 年后加 GCP，改动集中在 provider 层，不用动业务代码

  3. Python 团队的考虑点：
  - 团队都会 Python → 其实 CDK 也是候选（CDK Python 很成熟）
  - 但 CDK 只限 AWS → 1 年后要去 GCP 就走不通
  - 这个权衡在面试里说出来会显示你认真评估过："CDK 对 Python 团队学习曲线更低，但锁定 AWS；Terraform
  前期投入大，长期覆盖多云 — 选 TF 是为未来买保险。"

  ---
  Question 2:

  You're in the middle of running terraform apply on the production VPC when your laptop crashes. When you reboot and
  re-run terraform plan, Terraform shows it wants to destroy and recreate half the resources — even though you can see
  them running fine in the AWS Console.

  What happened, and what's your recovery plan? Walk me through step by step.

❯ 因为terraform apply执行了一半，所以导致state不一致，但是destroy and recreate是非常危险的，这时候应该用terreform
  import导入最新的状态，然后用terraform plan看最新的计划。必要的话要删除当前的state

● 方向正确（拒绝 destroy/recreate、用 import），但缺少关键的诊断步骤和更安全的恢复路径。反馈：

  ✅ 答对的部分：
  - 识别 destroy/recreate 是危险操作 — 核心判断正确
  - terraform import 重建 state — 是最后的 fallback
  - 先 plan 再 apply — 稳健

  ⚠️ 缺失的关键点：

  第一步 —— 先诊断到底发生了什么（你跳过了这步）：

  laptop crash 时，apply 被打断会留下两个问题：
  1. State lock 没释放 — DynamoDB 里 lock 还在，下次 plan/apply 会报 Error acquiring the state lock
  2. State file 可能没写回 S3 — apply 时 TF 会先在本地改 state，最后 upload 到 S3；如果 upload 前 crash，S3 上还是旧
  state

  这解释了为什么 plan 显示要 destroy/recreate —— 不是资源不见了，是 state 和现实不匹配。

  ---
  正确的恢复顺序：

  Step 1: 解锁 (如果需要)
    terraform force-unlock <LOCK_ID>
    (lock ID 会在错误信息里给出)

  Step 2: 检查 S3 state versioning (最重要!)
    aws s3api list-object-versions --bucket tf-state --prefix prod/vpc.tfstate
    → 看最近几个版本的时间戳
    → 找到 crash 前最后一个稳定版本

  Step 3: 用 plan 对比当前 state 和现实
    terraform plan
    → 如果显示要 destroy/recreate 几十个资源 = state 明显过时
    → 如果只是几个资源 drift = 可能是部分 apply 成功

  Step 4: 恢复策略（从安全到激进）

    4a. 【首选】从 S3 versioning 还原 state
        aws s3api copy-object \
          --copy-source "tf-state/prod/vpc.tfstate?versionId=<OLD_VERSION>" \
          --bucket tf-state --key prod/vpc.tfstate
        → 然后 terraform plan 确认一致

    4b. 【次选】用 terraform import 逐个补齐
        terraform import aws_vpc.main vpc-xxxxx
        terraform import aws_subnet.private_a subnet-xxxxx
        → 工作量大，适合无 versioning 的 state

    4c. 【绝对禁止】terraform apply 让它 destroy/recreate
        → 这会真的删 prod VPC

  ---
  关键概念修正：

  ▎ "必要的话要删除当前的state"

  这句话是危险的。删 state file ≠ 删资源，但：
  - rm terraform.tfstate → Terraform 以为没任何资源，下次 apply 会重建所有东西
  - 正确说法："把 state 里某个 resource 移除" 用 terraform state rm <resource>，之后再 terraform import 重新关联

  三个操作的区别（面试高频区分题）：

  ┌────────────────────┬────────────────────────────────────┬─────────────────────────────┐
  │        命令        │                作用                │      对实际资源的影响       │
  ├────────────────────┼────────────────────────────────────┼─────────────────────────────┤
  │ terraform destroy  │ 删除 state 里的资源 + 删除云上资源 │ 真的删 ⚠️                   │
  ├────────────────────┼────────────────────────────────────┼─────────────────────────────┤
  │ terraform state rm │ 只从 state 里移除记录              │ 云上资源不动 ✅             │
  ├────────────────────┼────────────────────────────────────┼─────────────────────────────┤
  │ terraform import   │ 把已有云资源加入 state             │ 云上资源不动，只建立关联 ✅ │
  └────────────────────┴────────────────────────────────────┴─────────────────────────────┘

  state rm + import 组合是修复 state drift 的标准手术刀。

  ---
  一句话升级版答法：

  ▎ "先 force-unlock 解锁，然后从 S3 state versioning 回滚到 crash 前的版本 —— 这是最安全的恢复。如果没 versioning，用
  ▎ terraform state rm + terraform import 逐个重建 state。绝不能让 plan 出现的 destroy/recreate 跑到 apply —— 那会真的删
  ▎  prod VPC。根因是 laptop crash 时 state 没写回 S3，所以 S3 versioning 是防这类事故的第一道防线。"

  ---
  准备好最后一题了。

  Question 3:

  Your team uses Terraform for AWS infra. A senior engineer on another team asks: "I need to create an S3 bucket right
  now for an urgent demo. Can I just click through the AWS Console? I'll add it to Terraform next week."

  How do you respond? What's the right process, and what enforcement mechanism would you put in place to prevent this
  from becoming a pattern?

✻ Sautéed for 30s

※ recap: AWS Infrastructure Module 7 (IaC) mock interview in progress. Just asked Q3 on how to respond to an engineer
  wanting to click-create an S3 bucket in Console instead of using Terraform. Waiting for your answer. (disable recaps
  in /config)

❯ 我会拒绝这样的要求，任何时候都应该避免直接操作AWS，因为这样的话导致drift，而且操作也没有记录，没有check，出了问题也没
  法回滚，问题太多了。这个是policy。但是我会要求team做一些module，使得一些常用操作很容易再terraform中实现，比如create
  s3 bucket。要避免这样的操作，我可以用IAM role的层面来禁止，只允许terreform 来操作AWS

● 满分级回答！三个关键点全部答到，且提出了两层防御（易用性 + 强制）。反馈：

  ✅ 全部答对：
  - 拒绝手动操作 — 态度正确
  - 识别三个核心问题：drift / 无审计 / 无法回滚 — 完整
  - Module 降低使用摩擦 — 这是最难的一点，很多人只会说"禁止"不会说"降低成本"
  - IAM 层面强制只让 Terraform 操作 — 真正的落地方案

  ---
  这题的答案已经很完整，补充两个让它"满分 +"的细节：

  1. 短期还是要解人家的急：

  面试官可能追问："那 demo 今天下午就要用怎么办？"

  好的答法：

  ▎ "短期方案：我帮他写一个最小 Terraform module，5 分钟 apply 完成，比他 Console 点还快。这既救急又建立了'TF
  ▎ 是最快路径'的认知 —— 如果工具比绕过它还慢，人就会绕过。工具好用是治本，强制是兜底。"

  2. IAM 强制的具体做法（面试官会追问"怎么实现"）：

  方案：区分 Human IAM vs CI IAM

  Human (via Identity Center):
    Permission Set = ReadOnly + 有限的"break-glass"角色
    → 日常只能看，不能改

  CI/CD (via OIDC federation):
    GitHub Actions 专用 IAM Role
    → 只有这个 Role 有 write 权限
    → 只能通过 Terraform apply 触发

  Break-glass 流程:
    生产事故需要手动改 → 走审批 → 临时 assume Admin Role
    → CloudTrail 全程记录 → 事后必须补 Terraform 代码
    → Slack 自动通知所有人

  关键原则：人没有直接 write 权限，只有"通过 PR + CI"才能改 prod。

  ---
  3. 配套的监测机制（提了会显示深度）：

  每日 drift detection:
    CronJob 跑 terraform plan -detailed-exitcode
    exit 2 (有 drift) → PagerDuty 告警
    → 立即调查：谁改的？CloudTrail 能找到人
    → 强制要么 import 到代码，要么 revert

  ---
  Module 7 总结得分：

  ┌──────────────────────────────┬────────────────────────────────────────────────────────────────────────┐
  │             题目             │                                掌握程度                                │
  ├──────────────────────────────┼────────────────────────────────────────────────────────────────────────┤
  │ Q1 IaC 选型 + 3 个月 rollout │ ⭐⭐⭐⭐ 选型正确，rollout 阶段性可再细化                              │
  ├──────────────────────────────┼────────────────────────────────────────────────────────────────────────┤
  │ Q2 State 损坏恢复            │ ⭐⭐⭐ 方向对，缺 S3 versioning + force-unlock + state rm/destroy 区分 │
  ├──────────────────────────────┼────────────────────────────────────────────────────────────────────────┤
  │ Q3 手动操作 policy           │ ⭐⭐⭐⭐⭐ 满分，两层防御（易用 + 强制）答得漂亮                       │
  └──────────────────────────────┴────────────────────────────────────────────────────────────────────────┘

  ---
  建议补强（面试前必看）：

  4. State 管理细节（Q2 是短板）：
  - S3 versioning 是 state 事故的第一道防线 —— 必须知道
  - force-unlock / state rm / import / destroy 四个命令的区别 —— 面试常考
  - State 里含明文 secret，IAM 必须严格限制 + 加密

  2. 选型权衡的"语言"：
  - 不要只说"多云选 Terraform"，要说**"为未来买保险"**
  - 不要只说"CDK 好"，要说**"Python 团队学习曲线低，但锁定 AWS"**
  - 面试官要的是权衡，不是结论

  3. 可能的追问方向：
  - "Terraform Cloud vs Atlantis vs 自建 GitHub Actions —— 你怎么选？"
  - "Module 版本升级怎么做 —— main 分支 vs tag？"
  - "Policy as Code 你会选 OPA 还是 Sentinel？"
  - "CloudFormation StackSets 和 Terraform + 多账号 state 相比有什么优劣？"

 1. State 到底是什么？为什么需要？

  核心认知：Terraform 是 declarative（声明式），你写的 .tf 文件描述"想要什么"，但 Terraform 怎么知道"现在有什么"？

  答案：State file (terraform.tfstate) —— 一个 JSON 文件，记录 Terraform 管理的每个资源的当前状态。

  .tf files (desired state)  ←──┐
                                │ plan 时对比
  current state (terraform.tfstate) ←─ sync from AWS API
                                │
  AWS actual resources ─────────┘

  State 的三个作用：

  2. 映射关系 —— aws_s3_bucket.my_bucket 这个 TF 资源对应 AWS 上的哪个 bucket（by ID/ARN）
  3. 性能优化 —— 不用每次都全量 query AWS API；state 里的信息直接用
  4. 依赖追踪 —— 资源之间的依赖关系，决定 create/destroy 的顺序

  没有 state 会怎样？
  - Terraform 不知道之前创建过什么 → 每次 apply 都认为要全部重建
  - 资源之间的关联丢失 → 无法做增量更新

  ---
  2. Local State vs Remote State

  Local state (默认)：
  terraform init  → 创建 terraform.tfstate 在当前目录

  致命问题：
  - 团队协作时，state 在你笔记本上，别人没有
  - 你笔记本坏了 → state 丢了 → Terraform 以为没有任何资源，下次 apply 会试图重建所有东西
  - 两个人同时 apply → state 冲突 → 损坏

  结论：生产环境必须用 Remote State。本地 state 只用于学习/个人实验。

  ---
  3. S3 + DynamoDB Backend 完整架构

  标准生产配置：

  terraform {
    backend "s3" {
      bucket         = "mycompany-tf-state"
      key            = "prod/network/vpc.tfstate"   # state 文件路径
      region         = "us-east-1"
      encrypt        = true                         # SSE-KMS 加密
      kms_key_id     = "arn:aws:kms:..."            # 用 CMK 做 key
      dynamodb_table = "tf-state-lock"              # 锁表
      versioning     = true                         # S3 versioning（实际在 bucket 上开）
    }
  }

  架构图：

  ┌────────────────────────────────────────────────┐
  │ S3 Bucket: mycompany-tf-state                  │
  │   ├── prod/network/vpc.tfstate      (+版本历史)│
  │   ├── prod/eks/cluster.tfstate      (+版本历史)│
  │   ├── prod/rds/aurora.tfstate       (+版本历史)│
  │   └── staging/...                              │
  │                                                 │
  │ 配置要求:                                       │
  │   ✓ Versioning: ON  (事故回滚必需)             │
  │   ✓ Encryption: SSE-KMS + CMK                  │
  │   ✓ Block Public Access: ON                    │
  │   ✓ Access logging: ON (审计)                  │
  │   ✓ IAM: 只有 TF CI Role + Admin 能读写        │
  └────────────────────────────────────────────────┘
                      ↕ read/write
  ┌────────────────────────────────────────────────┐
  │ DynamoDB Table: tf-state-lock                  │
  │   Partition Key: LockID (string)               │
  │   Billing: PAY_PER_REQUEST (几乎无成本)        │
  │                                                 │
  │ 作用: 防止并发 apply 损坏 state                │
  │   - apply 前写一条 LockID 记录                 │
  │   - apply 完删除                               │
  │   - 其他人看到 lock 存在 → 等或失败            │
  └────────────────────────────────────────────────┘

  面试常问：为什么 DynamoDB 而不是别的？
  - S3 不支持 conditional write（不能原子地"只在不存在时写入"）
  - DynamoDB 支持 ConditionExpression: attribute_not_exists(LockID) → 原生锁语义
  - 成本极低（几乎零）

  ---
  4. State Locking 机制详解

  正常流程：
  terraform apply
    ↓
  5. 尝试在 DynamoDB 写 Lock (ConditionExpression: 不存在才写)
    ↓
  6. 成功 → 执行 apply
    ↓
  7. apply 完成 → 删除 Lock 记录

  并发场景：
  Alice: terraform apply  ──→ 获得 lock
  Bob:   terraform apply  ──→ Error: state is locked
                                Lock ID: xxx-xxx
                                Who: alice
                                Created: 2026-04-21 10:00:00

  Lock 卡住的两种典型情况：

  ┌────────────────────┬─────────────────────────────┬──────────────────────────────────┐
  │        场景        │            原因             │               解决               │
  ├────────────────────┼─────────────────────────────┼──────────────────────────────────┤
  │ laptop crash       │ apply 中途死机，lock 没释放 │ terraform force-unlock <LOCK_ID> │
  ├────────────────────┼─────────────────────────────┼──────────────────────────────────┤
  │ 网络中断           │ apply 成功但删 lock 失败    │ 同上                             │
  ├────────────────────┼─────────────────────────────┼──────────────────────────────────┤
  │ CI job 超时被 kill │ 同 crash                    │ 同上                             │
  └────────────────────┴─────────────────────────────┴──────────────────────────────────┘

  force-unlock 的危险性：
  - 如果 apply 真的还在别处跑，你强制解锁 → 两个 apply 并发 → state 真的会损坏
  - 使用前必须确认没人在 apply（Slack 里问一圈、看 CI 状态、看 CloudTrail）

  ---
  5. S3 Versioning —— State 事故的救命稻草

  场景：某人误执行了 terraform destroy 删了生产 VPC。

  没有 versioning：state 里 VPC 被移除 + 云上 VPC 被删 → 重建要几天

  有 versioning：
  # Step 1: 找到 destroy 之前的 state 版本
  aws s3api list-object-versions \
    --bucket mycompany-tf-state \
    --prefix prod/network/vpc.tfstate

  # 输出示例:
  # Version xyz-123 (2026-04-21 10:05) ← destroy 后的空 state
  # Version abc-789 (2026-04-21 09:58) ← destroy 前的正常 state ✓

  # Step 2: 恢复到老版本
  aws s3api copy-object \
    --copy-source "mycompany-tf-state/prod/network/vpc.tfstate?versionId=abc-789" \
    --bucket mycompany-tf-state \
    --key prod/network/vpc.tfstate

  # Step 3: plan 确认
  terraform plan
  # → 应该显示 "No changes" (因为资源虽然在 AWS 被删了，但 state 还认为存在)
  # 或者显示要 create (因为资源真被删了)

  注意：state 恢复不等于资源恢复。 state 只是 Terraform 的"记账本"：
  - 如果资源还在 AWS 上（比如误删了 state 但没删资源）→ 恢复 state 就解决问题
  - 如果资源真的被 destroy 了 → 要么 apply 重建（可能换了 ID，新资源）；要么从快照/备份恢复

  强烈建议：state bucket 配 MFA Delete + Lifecycle 保留 90 天老版本。

  ---
  6. State 机密性 —— 被忽视的安全问题

  State 文件里会含有明文敏感信息：

  {
    "resources": [{
      "type": "aws_db_instance",
      "instances": [{
        "attributes": {
          "password": "my-plaintext-password",   ← 明文!!
          "endpoint": "xxx.rds.amazonaws.com"
        }
      }]
    }]
  }

  常见含敏感信息的资源：
  - aws_db_instance.password — RDS 密码
  - aws_secretsmanager_secret_version.secret_string — secret 内容
  - tls_private_key.private_key_pem — 私钥
  - random_password.result — 生成的随机密码

  防护措施：

  ┌──────────────────┬─────────────────────────────────────────────────────┐
  │       层级       │                        做法                         │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ S3 加密          │ SSE-KMS + CMK（不是默认的 SSE-S3）                  │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ KMS Key Policy   │ 只有 TF CI Role + 特定 Admin 能 decrypt             │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ S3 Bucket Policy │ aws:SecureTransport=true 强制 HTTPS                 │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ IAM              │ 严格限制 s3:GetObject on state bucket               │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ CloudTrail       │ 记录每次 state 访问，异常访问告警                   │
  ├──────────────────┼─────────────────────────────────────────────────────┤
  │ sensitive = true │ .tf 里标记敏感 output，避免 plan/apply 时打印到日志 │
  └──────────────────┴─────────────────────────────────────────────────────┘

  最佳实践：敏感数据不要让 Terraform 生成/保存，而是：
  # 不好: Terraform 生成密码，存在 state 里
  resource "random_password" "db" { length = 32 }

  # 好: 从 Secrets Manager 读取（state 里只有 ARN，不是密码本身）
  data "aws_secretsmanager_secret_version" "db_password" {
    secret_id = "prod/rds/master-password"
  }

  ---
  7. State 拆分策略 —— 按 Blast Radius

  反模式：一个 state file 管所有东西
  terraform/
  └── main.tf     ← 5000 行，VPC + EKS + RDS + 20 个微服务全在这
  问题：
  - plan 时间长（几分钟）
  - 改一个小东西，所有人要 review 全量
  - state 损坏 = 整个基础设施瘫痪
  - 任何人有 write 权限 = 能改所有东西

  正确拆分（按"变更频率 + blast radius"）：

  terraform/
  ├── foundation/              ← 几乎不变，改动需要 VP 批准
  │   ├── organizations/       (AWS Org、SCP)
  │   ├── network/             (VPC、TGW、Direct Connect)
  │   └── iam-baseline/        (角色、SCP、Identity Center)
  │
  ├── platform/                ← 季度变更
  │   ├── eks-cluster/         (EKS 控制面)
  │   ├── rds-aurora/          (Aurora 集群)
  │   └── kms-keys/
  │
  ├── platform-addons/         ← 月度变更
  │   ├── karpenter/
  │   ├── external-secrets/
  │   └── monitoring-stack/
  │
  └── applications/            ← 每日变更
      ├── team-a-services/
      ├── team-b-services/
      └── ...

  每个目录是独立 state file，独立 CI pipeline，独立 IAM Role。

  跨 state 引用（关键技巧）：

  # platform/eks-cluster/main.tf
  # 需要用 foundation/network 的 VPC ID

  data "terraform_remote_state" "network" {
    backend = "s3"
    config = {
      bucket = "mycompany-tf-state"
      key    = "foundation/network/vpc.tfstate"
      region = "us-east-1"
    }
  }

  resource "aws_eks_cluster" "main" {
    vpc_config {
      subnet_ids = data.terraform_remote_state.network.outputs.private_subnet_ids
    }
  }

  更推荐的方式（松耦合）：用 data source 直接查 AWS
  data "aws_vpc" "main" {
    tags = { Name = "prod-vpc" }
  }
  # 这样不依赖 remote state，state 搬家不影响

  ---
  8. State 操作命令完整清单

  面试必记的 6 个核心命令：

  ┌────────────────────────────────┬──────────────────────────────────┬─────────────────┬────────────────┐
  │              命令              │               作用               │ 对 state 的影响 │ 对云资源的影响 │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform state list           │ 列出 state 里所有资源            │ 只读            │ 无             │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform state show <res>     │ 显示某个资源的详细 state         │ 只读            │ 无             │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform state mv <src> <dst> │ 重命名 state 里的资源            │ 改 state        │ 无             │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform state rm <res>       │ 从 state 里移除（不删云资源）    │ 改 state        │ 无             │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform import <res> <id>    │ 把已有云资源加入 state           │ 改 state        │ 无             │
  ├────────────────────────────────┼──────────────────────────────────┼─────────────────┼────────────────┤
  │ terraform refresh              │ 从 AWS API sync 最新状态到 state │ 改 state        │ 无             │
  └────────────────────────────────┴──────────────────────────────────┴─────────────────┴────────────────┘

  对比 destroy 类命令：

  ┌─────────────────────────────────────────┬────────────┬──────────┬──────────┐
  │                  命令                   │    作用    │ 对 state │ 对云资源 │
  ├─────────────────────────────────────────┼────────────┼──────────┼──────────┤
  │ terraform destroy                       │ 删除所有   │ 清空     │ 真的删！ │
  ├─────────────────────────────────────────┼────────────┼──────────┼──────────┤
  │ terraform destroy -target=<res>         │ 删单个     │ 移除     │ 真的删！ │
  ├─────────────────────────────────────────┼────────────┼──────────┼──────────┤
  │ terraform apply（resource 从 .tf 移除） │ 删这个资源 │ 移除     │ 真的删！ │
  └─────────────────────────────────────────┴────────────┴──────────┴──────────┘

  ---
  9. 典型 State 场景与解法

  场景 A：把云上已有的手动创建资源纳入 TF 管理

  # Step 1: 写 .tf 代码描述这个资源
  resource "aws_s3_bucket" "existing" {
    bucket = "manually-created-bucket"
    # ... 其他属性
  }

  # Step 2: 告诉 TF 这个 .tf 资源对应哪个云资源
  terraform import aws_s3_bucket.existing manually-created-bucket

  # Step 3: plan 对比 .tf 和 state，补齐属性
  terraform plan
  # → 如果显示有 diff，修改 .tf 直到 plan 显示 "No changes"

  注意：
  - 新版 TF (1.5+) 支持 import block，可以声明式 import，更安全
  - 大规模 import 用 terraformer 工具自动生成

  场景 B：重命名资源（不想重建）

  # Before
  resource "aws_s3_bucket" "old_name" { ... }

  # After (改名)
  resource "aws_s3_bucket" "new_name" { ... }

  直接 apply → TF 会 destroy old + create new → 事故

  正确做法：
  terraform state mv aws_s3_bucket.old_name aws_s3_bucket.new_name

  场景 C：拆分 state（把资源从 state A 挪到 state B）

  # Step 1: 从 state A 移除（不删云资源）
  cd state-a/
  terraform state rm aws_s3_bucket.shared

  # Step 2: 在 state B 里 import
  cd ../state-b/
  terraform import aws_s3_bucket.shared actual-bucket-name

  # Step 3: 两边都 plan 确认

  场景 D：Backend 迁移（local → S3，或换 S3 bucket）

  # Step 1: 修改 backend 配置
  # Step 2: 重新 init
  terraform init -migrate-state
  # → TF 会自动把 local state 拷贝到新 backend

  场景 E：误删 state（或 state 损坏）

  # 选择 1: 从 S3 versioning 恢复（最快）
  aws s3api copy-object --copy-source "bucket/key?versionId=xxx" ...

  # 选择 2: 用 terraform refresh 重建（资源少时）
  terraform refresh
  # 但这要求 .tf 代码完整，且资源能被唯一识别

  # 选择 3: 逐个 import（资源多但 state 完全没了）
  for each resource: terraform import <res> <id>

  # 选择 4: 从备份恢复
  # (如果有定期备份 state 到另一个 bucket)

  ---
  10. State 相关的面试高频题

  Q1: 两个工程师同时 terraform apply 会发生什么？
  - DynamoDB lock 表会让第二个失败（获取不到锁）
  - 没配 DynamoDB lock 的话 → state race condition → state 可能损坏（新版本互相覆盖）

  Q2: State 可以提交到 Git 吗？
  - 绝对不行。原因：
    - 含明文敏感信息
    - 合并冲突无法解决（JSON 是机器生成的）
    - 无 locking 机制 → 并发 apply 损坏
  - state 的正确位置是加密的 S3 bucket + DynamoDB lock

  Q3: 生产环境 state 损坏怎么办？
  标准流程：
  1. 立即停止所有 apply（通知团队）
  2. 从 S3 versioning 恢复到最近稳定版本
  3. terraform plan 对比云上现状
  4. 如果有 drift，用 state rm + import 修复
  5. 事后复盘，看是否要加定期 state 备份到异地

  Q4: State 里的 password 怎么处理？
  - 理想：不让 TF 管密码，让 Secrets Manager 管，TF 只引用 ARN
  - 不得不管：S3 SSE-KMS + CMK 严格 IAM + CloudTrail 审计 + sensitive = true

  Q5: 一个 state 拆成多个，怎么决定拆分粒度？
  按 变更频率 + blast radius 拆：
  - 越稳定的资源放越底层（网络、IAM）
  - 变化快的放上层（应用）
  - 一个 state 的 plan 应该在 1-2 分钟内完成
  - 拆分粒度以"谁能改它"为准 —— 权限边界和 state 边界对齐

  Q6: terraform state rm vs terraform destroy 区别？
  - state rm —— 只是从 state 里"忘掉"这个资源，云上资源照常运行
  - destroy —— 真的删除云资源 + 从 state 移除
  - 典型用法：要把资源转给另一个 state 管理 → state rm 后再在新 state import

  Q7: 如果 Terraform 管理的资源被人 Console 手动改了？
  - 这叫 drift
  - terraform plan 会显示差异
  - 选择：
    - Revert —— apply 把手动改动覆盖回去
    - Accept —— 把改动同步到 .tf 代码
  - 预防：定期 drift detection + IAM 限制 human write 权限

  ---
  11. 一张表记住所有 State 管理要点

  ┌───────────────┬──────────────────────────────────────────┬──────────────────────────────────────────────┐
  │     主题      │                  关键点                  │                  面试一句话                  │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Remote state  │ S3 + DynamoDB 是标配                     │ "S3 存状态，DynamoDB 做锁，缺一不可"         │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Versioning    │ S3 versioning 是救命稻草                 │ "state 事故的第一道防线"                     │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Encryption    │ SSE-KMS + CMK，不是 SSE-S3               │ "state 含明文密码，必须 CMK 加密 + 严格 IAM" │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Locking       │ DynamoDB 的 conditional write 提供原子锁 │ "防并发 apply 损坏 state"                    │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ 拆分          │ 按 blast radius + 变更频率               │ "一个 state 只管一组同生命周期的资源"        │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ 跨 state 引用 │ 优先 data source，次选 remote state      │ "松耦合比紧耦合好"                           │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ 手术刀命令    │ state rm + import 是修复 drift 的组合    │ "忘记 + 重新记住，不动实际资源"              │
  ├───────────────┼──────────────────────────────────────────┼──────────────────────────────────────────────┤
  │ Force-unlock  │ 只在确认没人 apply 时用                  │ "盲目解锁 = 引入并发损坏风险"                │
  └───────────────┴──────────────────────────────────────────┴──────────────────────────────────────────────┘

  ---
  补强完成。这些细节覆盖了面试官在 State 管理主题上会问的 90% 的问题。

  Question 1:

  You joined a new team. Looking at their Terraform setup, you find:

  - State file is stored in S3 ✓
  - S3 bucket has SSE-S3 encryption (default AES256) ✓
  - Bucket versioning is OFF
  - No DynamoDB lock table configured
  - All 10 engineers have s3:GetObject and s3:PutObject on the state bucket via their Identity Center role

  List every problem you see, ordered by severity, and explain what could go wrong with each.

❯ 1. 去除s3:PutObject权限在Identify Center，这样避免任何人绕过terraform直接操作AWS
  2. 建立lock，没有lock很容易导致state crash
  3. 要有bucker version，这样除了问题，很容易找到上一个稳定版本恢复
  4. 要对S3 bucket做KMS-SSE的加密，这样更安全

● 方向都对，4 个问题全部识别到，但严重程度排序和修复逻辑需要调整。反馈：

  ✅ 答对的部分：
  - 4 个问题全部识别 — 没漏
  - 解决方向都对（lock / versioning / KMS / IAM 收紧）

  ⚠️ 严重程度排序需要修正：

  面试官想看到你按 "发生概率 × 影响" 排序，你的顺序反了。正确排序：

  ---
  🔴 CRITICAL #1 — 没有 DynamoDB Lock

  为什么最严重： 这是每天都可能触发的灾难。

  发生场景：
  - Alice 和 Bob 同时 terraform apply
  - 两人都读到同一份 state
  - 两人都各自修改并写回 S3
  - 后写的覆盖先写的 → state 里丢失资源记录 → 那些资源在 AWS 还在但 TF 不再管理 → 下次 apply 可能把它们删掉

  没有 lock 就等于：多人协作 × Terraform = 不定时炸弹

  ---
  🔴 CRITICAL #2 — 没有 Versioning

  为什么同等严重： 这是事故发生后的唯一救命稻草。没有它，事故就是不可恢复的。

  典型场景：
  - 上面那个 state 损坏事故发生了
  - 某人误执行 terraform destroy
  - state 文件被误删（aws s3 rm）
  - 以上任何一种 + 没 versioning = 只能逐个 import 几百个资源，恢复要几天

  Versioning 不是"好有",是灾难恢复的前提条件。

  ---
  🟠 HIGH #3 — SSE-S3 而不是 SSE-KMS + CMK

  为什么不是 CRITICAL： 数据泄露需要先攻破 IAM，是间接风险。

  但仍然严重： State 里含 RDS 密码、secret 内容、私钥。SSE-S3 的 key 由 AWS 管理，任何有 s3:GetObject 权限的人都能自动
  decrypt。

  换 SSE-KMS + CMK 的关键价值：
  - 除了 s3:GetObject，还需要 kms:Decrypt 权限
  - KMS Key Policy 可以精确控制谁能 decrypt（即使他有 S3 权限）
  - CloudTrail 记录每次 decrypt，能做审计

  这是双锁机制：S3 IAM 是第一把锁，KMS 是第二把锁。

  ---
  🟠 HIGH #4 — 10 个工程师直接有 S3 写权限

  你的描述有一个概念需要纠正：

  ▎ "去除 s3:PutObject 权限在 Identity Center，这样避免任何人绕过 terraform 直接操作 AWS"

  这句话混淆了两件事：
  - 绕过 Terraform 操作 AWS → 防这个要限制 AWS 资源的 write 权限（EC2/S3/RDS 等），不是 state bucket 权限
  - 直接写 state file → 这是另一个问题，防的是绕过锁机制 / 恶意篡改 state

  state bucket 权限的正确模型：

  ┌──────────────────────────────────┬─────────────────────────────────────────────┐
  │               角色               │              state bucket 权限              │
  ├──────────────────────────────────┼─────────────────────────────────────────────┤
  │ CI/CD Role (GitHub Actions OIDC) │ GetObject + PutObject + kms:Decrypt/Encrypt │
  ├──────────────────────────────────┼─────────────────────────────────────────────┤
  │ Human (通过 Identity Center)     │ 只要 GetObject（只读），用来本地 plan 排查  │
  ├──────────────────────────────────┼─────────────────────────────────────────────┤
  │ Admin / Break-glass              │ 临时 assume，有完整权限，走审批             │
  └──────────────────────────────────┴─────────────────────────────────────────────┘

  为什么工程师不需要 write？
  - apply 只能通过 CI 跑（这是前面 Q3 讨论过的策略）
  - 人只需要本地 terraform plan 来排查 → 只需要读 state
  - 没有 write 权限 = 不可能意外覆盖 state file、不可能绕过 lock 机制

  ---
  完整修复方案（按优先级）：

  Day 1 (立即) — 防事故扩大:
    1. 开 S3 Versioning + MFA Delete
    2. 创建 DynamoDB lock 表，更新 backend config
    3. 收紧 IAM: 人类 ReadOnly，apply 走 CI OIDC Role

  Week 1 — 加固:
    4. 迁移到 SSE-KMS + CMK，配 Key Policy 严格限制 decrypt
    5. S3 Bucket Policy: 强制 HTTPS (aws:SecureTransport=true)
    6. 开 S3 Access Logging，异常访问告警

  Week 2 — 治理:
    7. 所有其他 state file 做同样改造
    8. Lifecycle policy: 保留 90 天老版本
    9. 跨 region 备份 state bucket

  ---
  一句话总结severity排序逻辑：

  ▎ 发生概率 × 影响 排序：并发 apply（天天发生）> 恢复能力缺失（事故即不可逆）> 加密强度（需先攻破 IAM）>
  ▎ 权限范围（需要人为失误才触发）

  Question 2:

  Your team has this Terraform structure for production:

  prod/
  ├── main.tf          (800 lines)
  ├── vpc.tf
  ├── eks.tf
  ├── rds.tf
  ├── iam.tf
  ├── monitoring.tf
  └── backend.tf       (single state file: prod/terraform.tfstate)

  All prod resources are in ONE state file. terraform plan now takes 8 minutes. Any change to any file requires full
  team review. Last week, a bad S3 bucket change rolled back and took the entire VPC update with it.

  How do you refactor this? Give me the new structure, the migration approach, and the most important principle guiding
  your split.

✻ Cooked for 43s

※ recap: You're prepping AWS Infrastructure Module 7 (IaC) with state management mocks. Currently on Question 2:
  refactor a bloated 800-line single-state prod Terraform setup into properly split states. Next: answer Q2 with new
  structure, migration plan, and guiding principle.

❯ 应该根据变更频率+ domain范围来定义目录，每个目录下有不同的tf和tfstate。比如
  foundation - 基本上不变的，组织架构，IAM role, 网络划分，SCP
  platform - eks，rds各个子platform
  applications - 具体的应用
  可以设定对应的权限，以及CI/CD等，分别管理

● 方向完全对，结构分层也正确。但这题问了三件事（新结构 + 迁移方法 +
  核心原则），你的回答重点在"新结构"，迁移方法和核心原则表达得不够明确。反馈：

  ✅ 答对的部分：
  - 按变更频率 + domain 拆分 — 正确
  - foundation / platform / applications 三层结构 — 标准答案
  - 每个目录独立 tf + tfstate — 正确
  - 权限和 CI/CD 分别管理 — 正确

  ⚠️ 三个欠缺：

  ---
  欠缺 1: 核心原则（这是这题的"分数题"）

  你说"变更频率 + domain"，但面试官想听的关键词是 "Blast Radius"（爆炸半径）。

  完整表述：

  ▎ 核心原则：State 边界 = Blast Radius 边界 = 权限边界 = 变更节奏边界。四条边界对齐。
  ▎
  ▎ 也就是说，一个 state 里的所有资源应该：
  ▎ 1. 一起爆炸 —— 如果 state 损坏或误操作，影响范围你能接受
  ▎ 2. 一起变更 —— 变更频率相近（都月度 or 都每日）
  ▎ 3. 同一批人管 —— 有权改这些资源的人是同一组
  ▎ 4. 回滚影响一致 —— 回滚一个资源不会连累无关的东西

  你这个案例的原问题就是违反了第 4 条： S3 改动回滚拖累了 VPC —— 因为它们在同一个 state 里，同一次 apply，一起回滚。

  ---
  欠缺 2: 迁移方法（另一个重点）

  从"一个 state"到"多个 state"的迁移是手术，不是"重新写一遍 .tf"。

  正确迁移步骤（面试必答）：

  Step 1: 先建好目标结构（空的）
    foundation/network/ 目录，写好 .tf 文件
    配好新的 backend (不同 state key)
    terraform init (但不 apply)

  Step 2: 把资源从老 state "搬" 到新 state

    方案 A: state mv -state-out (单个文件内迁移)
      cd prod/
      terraform state mv \
        -state-out=../foundation/network/terraform.tfstate \
        aws_vpc.main aws_vpc.main
      → 从老 state 移除 + 加到新 state（不动云资源）

    方案 B: state rm + import (跨 backend 迁移)
      cd prod/
      terraform state rm aws_vpc.main

      cd ../foundation/network/
      terraform import aws_vpc.main vpc-xxxxx
      → 两步完成，更安全因为可以中途验证

  Step 3: 两边各跑 plan 验证
    cd foundation/network/ && terraform plan
    → 必须显示 "No changes"（资源已经在 state 里）

    cd prod/ && terraform plan
    → 必须显示 "No changes"（对剩下的资源）

  Step 4: 迁移顺序 — 从底层往上
    foundation/ → platform/ → applications/
    原因: 上层通过 data source / remote_state 依赖下层
          先迁下层，上层才能引用新位置

  关键注意：
  - 每次只迁一小部分（一个逻辑组），立即验证
  - 在低流量时段做（虽然不动资源，但 race condition 仍可能）
  - 做完整备份：迁移前复制一份老 state 到别处

  ---
  欠缺 3: 跨 state 引用（依赖方向）

  拆了之后，上层 state 怎么用下层 state 的资源？

  applications/team-a/
      ↓ 需要 VPC subnet IDs
  platform/eks/
      ↓ 需要 VPC ID
  foundation/network/

  两种方式（对比）：

  ┌────────────────────────┬────────────────────────────┬────────────────────────────────────────────────┐
  │          方式          │            代码            │                     优缺点                     │
  ├────────────────────────┼────────────────────────────┼────────────────────────────────────────────────┤
  │ terraform_remote_state │ 直接读下层 state output    │ 紧耦合，state 搬家会影响上层                   │
  ├────────────────────────┼────────────────────────────┼────────────────────────────────────────────────┤
  │ data source            │ 从 AWS API 查 data.aws_vpc │ 松耦合，state 搬家不影响；但要求资源有唯一 tag │
  └────────────────────────┴────────────────────────────┴────────────────────────────────────────────────┘

  推荐默认用 data source：
  # platform/eks/main.tf
  data "aws_vpc" "main" {
    tags = { Name = "prod-vpc" }
  }

  data "aws_subnets" "private" {
    filter {
      name   = "vpc-id"
      values = [data.aws_vpc.main.id]
    }
    tags = { Tier = "Private" }
  }
  这样就算 foundation state 拆分或搬家，platform 不用改一行代码。

  ---
  完整答案模板（面试可背）：

  ▎ 新结构（blast radius 分层）：
  ▎ foundation/   — 几乎不变，CISO/VP 批准才改
  ▎   ├── organizations/    (AWS Org、SCP)
  ▎   ├── network/          (VPC、TGW)
  ▎   └── iam-baseline/     (角色、KMS)
  ▎ platform/     — 季度变更，平台团队管
  ▎   ├── eks-cluster/
  ▎   ├── rds-aurora/
  ▎ platform-addons/ — 月度变更
  ▎   ├── karpenter/
  ▎   ├── monitoring/
  ▎ applications/ — 每日变更，业务团队管
  ▎   ├── team-a/
  ▎   └── team-b/
  ▎
  ▎ 迁移方法：自底向上，state rm + import 逐步搬，每步验证 plan 为空。
  ▎
  ▎ 核心原则：State 边界 = Blast Radius 边界 = 权限边界 = 变更节奏边界，四条边界对齐。一个 state 的 plan 应该 1-2
  ▎ 分钟内完成，一组人能独立 review 和 apply。

  Question 3（最后一题，最难）：

  It's Friday 4pm. A junior engineer runs terraform destroy in what he thought was the dev environment — but he was
  actually in the production/rds/ directory. The command completed 30 seconds ago. The RDS Aurora prod database
  (containing 6 months of ML experiment tracking data) is showing status "Deleting" in AWS Console.

  You get paged immediately. What do you do in the next 10 minutes? 1 hour? What's the long-term prevention?

  Walk me through specifically — include which tools, which commands, and which people you call.

✻ Brewed for 41s

❯ 1. next 10 minutes，首先停止当前destory，然后看看能不能rollback，因为是rds，如果删除了，可能数据也删除了，rollback需
  要考虑数据的恢复。rollback可以从s3拿到最近稳定版本，用plan看到底什么删除了，再决定怎么恢复，是不是需要force-unlock，
  是不是可以用apply
  2. 1 hour，如果恢复了，需要ML team验证他们的tracking data没丢，不然需要做数据库恢复
  3. 长远看，第一不能允许人来做这样的操作，可以用过IAM来限制，比如走CI/CD，而CI/CD会有validate/plan/apply，先apply到dev
  ，再apply到prod，apply之前必须有PR review。保证定时备份state，和rds数据。CloudTrail audit没有步骤，terrefrom plan
  -detailed-exitcode来查变更。

● 答到了核心思路（数据 vs state 分开考虑），但这道题有几个时间关键的细节需要抓住。反馈：

  ✅ 答对的部分：
  - 数据恢复 ≠ state 恢复 — 这是这题最容易混淆的点，你分开讨论是加分项
  - 长期预防方向全对：CI/CD + PR review + dev-first + state 备份 + CloudTrail
  - terraform plan -detailed-exitcode 做 drift detection — 正确

  ⚠️ 关键概念纠正：

  ---
  纠正 1: "首先停止当前 destroy" — 做不到

  题目说 "completed 30 seconds ago"，命令已经返回。Terraform 的 destroy 是异步的：
  - TF 调用 AWS API DeleteDBInstance → API 立即返回 → TF 命令结束
  - AWS 后台执行删除 → RDS 显示 "Deleting" → 几分钟后真正消失

  你能控制的不是"停止 destroy"，而是"抢救数据"。 每一分钟都重要。

  ---
  纠正 2: S3 Versioning 救 state，不救 数据

  你说的 "从 S3 拿到最近稳定版本" 只能恢复 Terraform 的记账本，恢复不了 RDS 数据本身。

  两条独立的恢复路径：

  数据恢复路径 (紧急!):          State 恢复路径 (之后再做):
  AWS 自动备份 / PITR            S3 versioning 回滚
      ↓                              ↓
  新建 RDS 实例从备份恢复         state 里重新有了 RDS 记录
      ↓                              ↓
  应用连到新实例                   terraform plan 对比新老

  这两条路径的工具、时间、负责人都不一样。 面试官想看你能分开处理。

  ---
  正确的 10 分钟 / 1 小时 / 长期回应

  🚨 前 10 分钟 — 全部是"动作"，不是"调查"

  T+0 (事故发生)
    ↓
  T+1min  立即行动 (并行):
    □ 页 DBA / 数据团队 (PagerDuty + Slack @here)
    □ 页 ML platform on-call (他们的应用会开始报错)
    □ 开 war room (Zoom + Slack incident channel)

  T+2min  检查数据状态 (AWS Console 或 CLI):
    aws rds describe-db-clusters --db-cluster-identifier prod-ml-tracking

    三种可能 (按好到坏):
    情况 A: status=deleting + final-snapshot 存在 ✅
            → 从 snapshot 新建集群 (RestoreDBClusterFromSnapshot)
    情况 B: status=deleting + 无 final snapshot ⚠️
            → 立即用 Automated Backup PITR 恢复到 T-1min
    情况 C: 已经完全消失 ❌
            → 只能 PITR 恢复到删除前的时间点

  T+3min  启动数据恢复 (DBA 操作):
    aws rds restore-db-cluster-to-point-in-time \
      --source-db-cluster-identifier prod-ml-tracking \
      --db-cluster-identifier prod-ml-tracking-restored \
      --restore-to-time 2026-04-21T15:59:00Z    ← 删除前 1 分钟

    注意: 恢复出来的是新 cluster (不同 endpoint)
    Aurora 的 PITR 窗口默认 1-35 天，恢复需要 15-30 分钟

  T+5min  通知下游:
    □ 所有依赖这个 RDS 的 service 会开始报错
    □ Status page 更新
    □ 业务方通知

  T+10min 准备切换连接:
    恢复出来的 cluster endpoint 是新的 (prod-ml-tracking-restored.xxx.rds...)
    需要:
    □ 更新 Secrets Manager 里的 DB_HOST
    □ 应用重启 (External Secrets Operator 会自动同步)
    □ 或改 Route 53 CNAME 指向新 endpoint (最快，无需改应用)

  ---
  ⏱️ 接下来 1 小时 — 稳定 + 根因

  T+30min  数据恢复完成验证:
    □ DBA 连新 cluster，spot check 数据
    □ ML team 验证 experiment tracking 数据完整
    □ 应用逐步切换流量到新 cluster

  T+45min  State 恢复 (优先级较低，可以慢):
    cd production/rds/

    # 从 S3 versioning 找到 destroy 前的 state
    aws s3api list-object-versions --bucket tf-state \
      --prefix production/rds/terraform.tfstate

    # 但!! 恢复的 state 指向的是旧 cluster ID，旧 cluster 已经不存在
    # 正确做法:
    # 1. 从 S3 恢复 state
    # 2. terraform state rm aws_rds_cluster.main
    # 3. terraform import aws_rds_cluster.main prod-ml-tracking-restored
    # 4. terraform plan → 应该 No changes

  T+60min  开始 post-mortem:
    □ 写 incident timeline
    □ 计算 RTO (实际恢复时间) / RPO (数据丢失时间点)
    □ 通知所有受影响方

  ---
  🛡️ 长期预防 — 你答对了方向，但缺了最关键的两个技术控制

  你说的"不能允许人来做这样的操作 + CI/CD + PR review" —— 正确，但治本还要加：

  1. 资源层面的硬保护（最重要！）：

  # A) RDS 的 deletion_protection
  resource "aws_rds_cluster" "main" {
    cluster_identifier  = "prod-ml-tracking"
    deletion_protection = true    ← AWS 层面，TF 的 destroy 也删不掉
    skip_final_snapshot = false   ← 删除前强制创建 snapshot

    lifecycle {
      prevent_destroy = true       ← Terraform 层面，destroy 会直接报错退出
    }
  }

  两层保护叠加：
  - prevent_destroy → Terraform 在 plan 阶段就拒绝
  - deletion_protection → 即使有人绕过 TF 直接调 API 也删不了

  这位 junior 工程师的 destroy 在哪一层会被挡住？
  - 如果有 prevent_destroy → TF 命令直接失败，根本不会发 API → 事故不存在
  - 如果只有 deletion_protection → TF 调 API，AWS 拒绝 → 事故不存在

  面试关键点： 生产 stateful 资源（RDS、S3 带数据的 bucket、DynamoDB）必须这两层都开。

  ---
  2. 环境彻底物理隔离：

  不是: 同一账号下 dev/ prod/ 目录
  而是: 独立 AWS 账号 + 独立 Identity Center Permission Set

  工程师登录时必须主动切账号:
    aws sso login --profile dev        ← 能做 destroy
    aws sso login --profile prod       ← 只有 ReadOnly，没 destroy 权限

  这位 junior 为什么能 destroy prod？ 因为他 AWS CLI 配置里有 prod 账号的 write credentials。正确架构下，人在 prod
  账号只有 ReadOnly。 destroy 只能通过 CI Role 触发，而 CI 的触发需要 PR merge。

  ---
  3. 完整的防御层次（面试必答）：

  第 1 层 — 文件层: Terraform prevent_destroy lifecycle
  第 2 层 — API 层: AWS deletion_protection + final_snapshot
  第 3 层 — 权限层: 人在 prod 账号只读，destroy 只能 CI Role
  第 4 层 — 流程层: CI/CD + PR review + dev→staging→prod
  第 5 层 — 恢复层: PITR 窗口 35 天 + 跨 region 备份 + 定期演练
  第 6 层 — 监测层: CloudTrail 异常删除告警 + GuardDuty

  单一防线都不够，必须多层纵深。

  ---
  这题的"一句话升级版"答法：

  ▎ 10 分钟： 页 DBA + ML on-call，并行启动 RDS PITR 到删除前 1 分钟，这是数据能否救回来的唯一时间窗口 —— Terraform
  ▎ state 恢复可以等，数据恢复不能等。
  ▎
  ▎ 1 小时： 数据恢复完成后切换应用连接（改 Secrets Manager 或 Route 53 CNAME），然后用 state rm + import 修复 state
  ▎ 指向新 cluster。
  ▎
  ▎ 长期： 真正的治本是资源级硬保护 —— RDS deletion_protection + TF prevent_destroy + 人在 prod 账号只读。CI/CD 和
  ▎ review 是流程层，技术控制比流程控制更可靠。

  ---
  Module 7 State 管理 mock 总结得分：

  ┌─────────────────────────┬──────────────────────────────────────────────────────┐
  │          题目           │                       掌握程度                       │
  ├─────────────────────────┼──────────────────────────────────────────────────────┤
  │ Q1 Backend 配置问题识别 │ ⭐⭐⭐⭐ 识别全，severity 排序和 IAM 概念需调整      │
  ├─────────────────────────┼──────────────────────────────────────────────────────┤
  │ Q2 State 拆分重构       │ ⭐⭐⭐⭐ 结构对，原则和迁移方法表达要更明确          │
  ├─────────────────────────┼──────────────────────────────────────────────────────┤
  │ Q3 生产 destroy 事故    │ ⭐⭐⭐ 思路分层对，缺 RDS 技术细节和 prevent_destroy │
  └─────────────────────────┴──────────────────────────────────────────────────────┘

  ---
  建议补强（最后一次）：

  必须记住的 3 个关键概念：

  1. Blast Radius — 这是 state 拆分的唯一指导原则，面试回答要明确说出这 4 个字的英文
  2. 数据恢复 vs State 恢复 — 两条独立路径，用不同工具，有不同紧急程度
  3. prevent_destroy + deletion_protection — 生产 stateful 资源的双保险，记不住这两个关键字就等于没学

  可能的追问：
  - "prevent_destroy 和 deletion_protection 重复吗？为什么两个都要？"
  - "RDS PITR 窗口是多久？如果需要恢复 40 天前的数据怎么办？"（答：35 天默认，更长要手动 snapshot + 跨 region 备份）
  - "恢复出来的 cluster endpoint 变了，有哪些无缝切换的做法？"（答：Route 53 CNAME / Secrets Manager 改 DB_HOST / RDS
  Proxy）
  这是 AI Infra Manager 面试最重头的模块。GPU 选型、Spot 策略、Karpenter 是高频必考。

  ---
  1. 为什么 Compute 是 AI Infra 的第一优先级

  AI 公司 AWS 账单 60-70% 是 GPU 计算。 一个决策（选 P4d 还是 P5、用 Spot 还是 On-Demand、Karpenter 还是 Managed Node
  Group）可能每月影响几十万美金。

  所以这个模块面试官会问得特别深，要求你能:
  - 准确说出 GPU 实例型号和适用场景
  - 给出 Spot 策略的完整落地方案
  - 解释 EKS 节点管理的架构选择

  ---
  2. EC2 GPU 实例家族（必背）

  2.1 训练 Instance Types

  ┌──────────┬───────────────┬────────┬───────────────────────┬───────────┬───────────────────────────────────────┐
  │   家族   │      GPU      │  GPU   │       GPU 互联        │   网络    │               典型用途                │
  │          │               │  数量  │                       │           │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ P5       │ H100 SXM5     │ 8      │ NVLink + NVSwitch     │ 3200 Gbps │ LLM 训练首选，大模型预训练            │
  │          │ (80GB)        │        │ (900 GB/s)            │  EFA      │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ P5e /    │ H200 (141GB)  │ 8      │ NVLink + NVSwitch     │ 3200 Gbps │ 最新，H200 显存更大                   │
  │ P5en     │               │        │                       │  EFA      │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ P4d      │ A100 SXM4     │ 8      │ NVLink (600 GB/s)     │ 400 Gbps  │ A100 训练标配，仍主流                 │
  │          │ (40GB)        │        │                       │ EFA       │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ P4de     │ A100 SXM4     │ 8      │ NVLink                │ 400 Gbps  │ A100 80GB，大显存训练                 │
  │          │ (80GB)        │        │                       │ EFA       │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ P3dn     │ V100 (32GB)   │ 8      │ NVLink                │ 100 Gbps  │ 老一代，仍有人用                      │
  │          │               │        │                       │ EFA       │                                       │
  ├──────────┼───────────────┼────────┼───────────────────────┼───────────┼───────────────────────────────────────┤
  │ Trn1 /   │ Trainium      │ 16 /   │ NeuronLink            │ 800 Gbps  │ AWS 自研芯片，便宜 30-50%，但需改     │
  │ Trn2     │               │ 32     │                       │ EFA       │ PyTorch/XLA                           │
  └──────────┴───────────────┴────────┴───────────────────────┴───────────┴───────────────────────────────────────┘

  2.2 推理 Instance Types

  ┌──────────┬─────────────────────┬────────────────────┬───────────────────────────┐
  │   家族   │     GPU / 芯片      │        特点        │         典型用途          │
  ├──────────┼─────────────────────┼────────────────────┼───────────────────────────┤
  │ G5       │ A10G (24GB) × 1/4/8 │ 性价比高           │ 中小模型推理、LLM < 20B   │
  ├──────────┼─────────────────────┼────────────────────┼───────────────────────────┤
  │ G6 / G6e │ L4 / L40S           │ 更新，比 G5 效率高 │ 推理、轻量训练            │
  ├──────────┼─────────────────────┼────────────────────┼───────────────────────────┤
  │ Inf2     │ Inferentia2         │ AWS 自研，成本低   │ 大规模推理，需 Neuron SDK │
  ├──────────┼─────────────────────┼────────────────────┼───────────────────────────┤
  │ P5 / P4d │ H100 / A100         │ 训练卡也能推理     │ 超大模型（70B+）推理      │
  └──────────┴─────────────────────┴────────────────────┴───────────────────────────┘

  2.3 关键数字（面试背诵）

  P5.48xlarge  (H100 × 8, 3200 Gbps EFA): ~$98/hr On-Demand
  P4d.24xlarge (A100 × 8, 400 Gbps EFA):  ~$32/hr On-Demand
  G5.48xlarge  (A10G × 8):                ~$16/hr On-Demand
  Inf2.48xlarge (Inferentia2 × 12):       ~$12/hr On-Demand

  Spot 通常是 On-Demand 的 30-50%，GPU Spot 最多省 70%

  2.4 选型决策（面试高频）

  训练大模型 (>10B 参数, 多节点)?
  ├── 预算充足, 要最快 → P5 (H100)
  ├── 预算中等 → P4d (A100)
  └── 成本敏感, 愿意改代码 → Trn1/Trn2 (Trainium)

  推理?
  ├── 小模型 (<7B) → G5 / G6
  ├── 大模型 (>30B) → P4d / P5 (需要多卡)
  ├── 海量低成本推理 → Inf2 (Inferentia2)
  └── 边缘 / 低延迟 → G6 + GPU 直通优化

  微调 (Fine-tuning)?
  └── G5 (单机 A10G) 足够, 或 P4d 单卡用

  ---
  3. Spot Instance 策略 — AI Infra 成本优化的最大杠杆

  3.1 为什么 GPU Spot 这么重要

  P4d On-Demand: $32/hr × 8 卡 × 24 × 30 = $23K/月/实例
  P4d Spot:      $10/hr × 8 卡 × 24 × 30 = $7K/月/实例

  1000 张 A100 规模:
    On-Demand: $23M/月
    Spot:      $7M/月
    差额:      $16M/月 ≈ $200M/年 💰

  结论：能不能用好 Spot，决定公司 AI Infra 账单是 $10M 还是 $30M/年。

  3.2 Spot 的本质

  - AWS 把空闲容量以最高 90% 折扣卖出
  - 2 分钟中断通知（CloudWatch Event / Instance Metadata）
  - 中断后立即回收，你的 job 被杀
  - 无 SLA（理论上任何时候都可能被回收）

  3.3 Spot 能用的核心前提 — Checkpoint-Resume

  训练代码必须支持：

  # 伪代码
  for epoch in range(num_epochs):
      for batch in dataloader:
          loss = model(batch)
          optimizer.step()

      # 每 N 步 checkpoint 到 S3 (或 FSx -> S3)
      if step % checkpoint_freq == 0:
          save_checkpoint(
              model, optimizer, scheduler, step,
              path=f"s3://ckpt/job-123/step-{step}.pt"
          )

  # 启动时优先从 checkpoint 恢复
  if checkpoint_exists():
      model, optimizer, start_step = load_checkpoint()
  else:
      start_step = 0

  Checkpoint 频率权衡：
  - 太频繁 → IO 开销大，训练变慢
  - 太稀疏 → Spot 中断丢失进度多
  - 经验值：每 15-30 分钟一次（正好匹配 Spot 典型存活时间）

  3.4 Spot Interruption Handler（必考）

  在节点上跑一个 DaemonSet，监听 Spot 中断通知：

  # AWS Node Termination Handler
  apiVersion: apps/v1
  kind: DaemonSet
  metadata:
    name: aws-node-termination-handler
  spec:
    template:
      spec:
        containers:
        - name: handler
          image: public.ecr.aws/aws-ec2/aws-node-termination-handler
          env:
          - name: ENABLE_SPOT_INTERRUPTION_DRAINING
            value: "true"
          - name: DRAIN_GRACE_PERIOD
            value: "120"   # 2分钟

  触发流程：
  AWS 发 Spot 中断通知 (2 分钟)
      ↓
  Node Termination Handler 检测到
      ↓
  cordon node (不再调度新 pod)
      ↓
  drain node (kubectl drain):
      ├── 发 SIGTERM 到 pod
      ├── Pod preStop hook 触发:
      │   └── 保存 emergency checkpoint 到 S3
      └── Pod 收到 SIGTERM → 优雅退出
      ↓
  K8s scheduler 在其他节点重启 Pod
      ↓
  Pod 从 S3 最新 checkpoint 恢复训练

  3.5 Spot 多样化策略（Diversification）

  问题： 如果所有 Pod 都要 p4d.24xlarge，AWS 某个 AZ 的 p4d Spot 池耗尽 → 全部中断。

  解法： 用 EC2 Fleet 或 Karpenter 配置多个可接受的实例类型：

  # Karpenter NodePool
  spec:
    template:
      spec:
        requirements:
        - key: node.kubernetes.io/instance-type
          operator: In
          values:
          - p4d.24xlarge
          - p4de.24xlarge
          - p5.48xlarge        # 能接受用更贵的也行
        - key: karpenter.sh/capacity-type
          operator: In
          values: ["spot", "on-demand"]   # Spot 首选, On-Demand fallback
        - key: topology.kubernetes.io/zone
          operator: In
          values: ["us-east-1a", "us-east-1b", "us-east-1c", "us-east-1d"]

  Allocation Strategy（分配策略）：
  - capacity-optimized — 选择当前容量最稳定的 Spot 池（推荐）
  - lowest-price — 选最便宜的（容易集中在一个池，易被中断）
  - capacity-optimized-prioritized — 有优先级的 capacity-optimized

  3.6 Spot + On-Demand 混合架构

  典型架构：

  Job 优先级:
  ├── Critical (不能中断, 如 final training run)
  │   └── On-Demand 100% (+ Reserved Instance / Savings Plan)
  │
  ├── Normal training (可接受中断, 有 checkpoint)
  │   └── Spot 70% + On-Demand 30% (fallback)
  │
  └── Experiment / Research
      └── Spot 100% (最便宜，能跑完就跑完)

  Karpenter NodePool 用 weight 控制优先级：
  # 优先 Spot
  - nodePoolRef: spot-gpu
    weight: 100
  # Spot 不够时 fallback 到 On-Demand
  - nodePoolRef: on-demand-gpu
    weight: 10

  ---
  4. Savings Plans vs Reserved Instances

  4.1 Savings Plans（推荐）

  三种类型：

  ┌────────────────────────────┬─────────────────────────────────────────────┬──────────┬────────────────┐
  │            类型            │                  覆盖范围                   │   折扣   │     灵活性     │
  ├────────────────────────────┼─────────────────────────────────────────────┼──────────┼────────────────┤
  │ Compute Savings Plans      │ EC2 + Fargate + Lambda，任何实例族 / region │ 最高 66% │ 最灵活（推荐） │
  ├────────────────────────────┼─────────────────────────────────────────────┼──────────┼────────────────┤
  │ EC2 Instance Savings Plans │ 特定实例族，特定 region                     │ 最高 72% │ 锁定实例族     │
  ├────────────────────────────┼─────────────────────────────────────────────┼──────────┼────────────────┤
  │ SageMaker Savings Plans    │ SageMaker 相关                              │ —        │ SageMaker 专用 │
  └────────────────────────────┴─────────────────────────────────────────────┴──────────┴────────────────┘

  关键规则：
  - 承诺 $X/hour 的固定消费，1 或 3 年
  - 实际用的 compute 按承诺额自动打折
  - 超出部分按 On-Demand 价
  - 没用完 — 钱照样花（用不掉就是浪费）

  4.2 Savings Plans vs Spot 的关系

  误区：以为两者二选一。

  正确架构 — 两者互补：

  Baseline 负载 (inference serving, 24/7 运行):
      ↓ 用 Savings Plans 锁定 1-3 年, 省 30-60%

  Burst / 训练 (可中断, 变动大):
      ↓ 用 Spot Instance, 省 70%

  Savings Plans 只覆盖 On-Demand 那部分, Spot 不受影响.
  Spot 运行的实例也算你用了 compute, 但 Savings Plans 不覆盖 Spot.

  4.3 购买 Savings Plans 的方法论

  Step 1: 看过去 3-6 个月 Cost Explorer 的 On-Demand baseline
          → 找 P50 的消费水平 (50% 时间高于, 50% 低于)

  Step 2: 按 P50 买 Compute Savings Plans (1 年)
          → 保证利用率 > 95%

  Step 3: Burst 部分继续 On-Demand 或 Spot
          → 不要把 Savings Plan 买到峰值 (会浪费)

  3 年 Savings Plans 折扣最多, 但需要业务稳定性判断.

  ---
  5. EKS Node 管理 — 三种架构对比

  5.1 Managed Node Groups（MNG）

  AWS 托管的节点池，最简单。

  你只管:
    ├── 指定实例类型 (如 p4d.24xlarge)
    ├── min/max/desired 节点数
    └── Subnet, IAM role
  AWS 帮你:
    ├── 节点启动 / 注册到集群
    ├── 节点升级 (但要你触发)
    └── 节点替换 (失败时)

  优点： 简单、AWS 支持、GUI 友好
  缺点：
  - 扩缩容慢（分钟级）
  - 只能预设固定实例类型（不灵活）
  - Spot 多样化支持有限
  - 不会 bin-pack（即使只需 1 个 pod 也起整台机器）

  适合： 小规模、稳定负载、不想折腾

  ---
  5.2 Cluster Autoscaler (CAS) + Self-managed Node Groups

  经典方案，在 Karpenter 之前是主流。

  - 你定义 Auto Scaling Group（ASG）
  - CAS 监控 Pending pod → 决定扩容哪个 ASG
  - ASG 加节点

  问题：
  - 每个 ASG 只能一种实例类型 → 需要建 N 个 ASG 对应 N 种实例
  - 扩缩容慢（2-5 分钟）
  - 容易过度配置（pod 需要 1 GPU, ASG 里只有 8 GPU 的机器 → 浪费 7 个）

  现状： 已被 Karpenter 取代，新项目不推荐。

  ---
  5.3 Karpenter（当前最佳方案）⭐

  AWS 开源，专为 K8s 设计的 just-in-time provisioner。

  Karpenter 工作流程:

  1. Pod pending, 资源不足
  2. Karpenter 计算: 什么实例能放下这个 pod?
  3. 在能放下的实例里选最便宜的 (bin-packing)
  4. 直接起 EC2 实例 (不走 ASG!)
  5. 实例启动, 加入集群, pod 调度上去
  6. 其他 pod 过来, 继续 bin-pack 到已有节点
  7. 节点空闲 (consolidation) → Karpenter 主动缩容

  核心优势：

  ┌─────────────┬─────────────────────┬───────────────────────────────────┐
  │    维度     │         CAS         │             Karpenter             │
  ├─────────────┼─────────────────────┼───────────────────────────────────┤
  │ 扩容速度    │ 2-5 分钟            │ 30-60 秒                          │
  ├─────────────┼─────────────────────┼───────────────────────────────────┤
  │ 实例多样性  │ 一个 ASG = 一种实例 │ 一个 NodePool 可以接受几十种实例  │
  ├─────────────┼─────────────────────┼───────────────────────────────────┤
  │ Spot 多样化 │ 差                  │ 极好 (自动跨 AZ + 跨实例类型)     │
  ├─────────────┼─────────────────────┼───────────────────────────────────┤
  │ Bin-packing │ 无                  │ 原生支持                          │
  ├─────────────┼─────────────────────┼───────────────────────────────────┤
  │ 配置方式    │ ASG + CAS config    │ K8s CRD (NodePool + EC2NodeClass) │
  └─────────────┴─────────────────────┴───────────────────────────────────┘

  5.4 Karpenter NodePool 配置示例

  # NodePool: 调度策略
  apiVersion: karpenter.sh/v1
  kind: NodePool
  metadata:
    name: gpu-training
  spec:
    template:
      metadata:
        labels:
          workload-type: gpu-training
      spec:
        nodeClassRef:
          name: default
        requirements:
        - key: node.kubernetes.io/instance-type
          operator: In
          values: [p4d.24xlarge, p4de.24xlarge, p5.48xlarge]
        - key: karpenter.sh/capacity-type
          operator: In
          values: [spot, on-demand]
        taints:
        - key: nvidia.com/gpu
          value: "true"
          effect: NoSchedule

    limits:
      nvidia.com/gpu: 128       # 整个 NodePool 最多 128 张 GPU

    disruption:
      consolidationPolicy: WhenUnderutilized
      consolidateAfter: 30s     # 空闲 30s 就考虑合并/缩容
      expireAfter: 720h         # 节点最长存活 30 天 (强制重建, 更新 AMI)

  ---
  # EC2NodeClass: AWS 层配置
  apiVersion: karpenter.k8s.aws/v1
  kind: EC2NodeClass
  metadata:
    name: default
  spec:
    amiFamily: AL2023
    role: "KarpenterNodeRole"
    subnetSelectorTerms:
    - tags:
        karpenter.sh/discovery: "my-cluster"
    securityGroupSelectorTerms:
    - tags:
        karpenter.sh/discovery: "my-cluster"
    blockDeviceMappings:
    - deviceName: /dev/xvda
      ebs:
        volumeSize: 500Gi
        volumeType: gp3

  5.5 Karpenter 的 Consolidation（缩容）

  场景： 10 个 pod 分散在 5 个节点（每个节点 2 pod），但其实 3 个节点就够。

  Karpenter 自动：
  8. 识别出可以合并
  9. 起新节点（能装下所有 pod）
  10. drain 旧节点，pod 迁移
  11. 删除旧节点

  好处： 长期运行的集群不会浪费资源。
  注意： 有状态负载（训练中的 GPU job）会被中断 → 用 karpenter.sh/do-not-disrupt annotation 保护。

  ---
  12. GPU 在 EKS 上的调度

  6.1 NVIDIA Device Plugin

  让 K8s 能"看到"GPU。

  # DaemonSet on every GPU node
  nvidia-device-plugin:
    exposes:  nvidia.com/gpu: 8   (P4d 有 8 张 GPU)

  Pod 请求:
    resources:
      limits:
        nvidia.com/gpu: 2        # 这个 pod 要 2 张 GPU

  6.2 GPU 共享 / 切分

  问题： 一张 A100 给一个推理 pod 可能利用率只有 20%，浪费 80%。

  三种共享方案：

  ┌─────────────────────────────┬───────────────────────────────────┬──────────┬──────────────────┐
  │            方案             │               原理                │   隔离   │       适用       │
  ├─────────────────────────────┼───────────────────────────────────┼──────────┼──────────────────┤
  │ Time-slicing                │ 多 pod 轮流用同一张 GPU           │ 无隔离   │ dev/test, 小推理 │
  ├─────────────────────────────┼───────────────────────────────────┼──────────┼──────────────────┤
  │ MIG (Multi-Instance GPU)    │ A100/H100 硬件级切成 7 个独立实例 │ 硬件隔离 │ 生产推理         │
  ├─────────────────────────────┼───────────────────────────────────┼──────────┼──────────────────┤
  │ MPS (Multi-Process Service) │ 多进程并发用 GPU                  │ 软件隔离 │ 中等             │
  └─────────────────────────────┴───────────────────────────────────┴──────────┴──────────────────┘

  MIG 配置示例：
  # NVIDIA GPU Operator 配置
  mig:
    strategy: single
    profile: "1g.5gb"        # 一张 A100 切成 7 个 5GB 实例

  Pod 请求:
    resources:
      limits:
        nvidia.com/mig-1g.5gb: 1   # 申请一个 MIG 实例

  6.3 多节点训练 —— GPU Job + Volcano/Kueue

  问题： 8 节点训练任务，K8s 默认是"pod by pod"调度，可能起了 6 个 pod 后资源不够，前 6 个白起了浪费 GPU。

  解法：Gang Scheduling
  - Volcano — 开源 batch scheduler
  - Kueue — K8s SIG 官方 batch scheduler（2024 后推荐）

  # Volcano Job
  apiVersion: batch.volcano.sh/v1alpha1
  kind: Job
  spec:
    minAvailable: 8    # 必须 8 个 pod 同时起, 否则不调度
    tasks:
    - replicas: 8
      template:
        spec:
          containers:
          - image: training-image
            resources:
              limits:
                nvidia.com/gpu: 8

  Gang Scheduling 保证： 要么所有 pod 一起起，要么一个都不起。

  ---
  7. EFA + Cluster Placement Group（多节点训练必需）

  7.1 为什么多节点训练需要 EFA

  普通 TCP/IP 网络：
  - 100 Gbps 网卡看起来够快
  - 但有 OS kernel overhead，latency 几十微秒
  - 多节点 all-reduce 时延叠加 → GPU 等网络 → 利用率低

  EFA (Elastic Fabric Adapter)：
  - OS-bypass（RDMA-like）
  - Latency 微秒级
  - 与 NCCL 集成（NVIDIA Collective Communications Library）
  - P4d: 400 Gbps，P5: 3200 Gbps

  7.2 EFA 使用前提（必考）

  1. 实例类型支持 EFA (p4d, p4de, p5, trn1 等; g5 不支持)
  2. 安装 EFA driver (AMI 里预装, 或手动装)
  3. NCCL 配置:
     export FI_PROVIDER=efa
     export NCCL_IBEXT_DISABLE=1
  4. Security Group: 允许所有节点之间的流量 (all protocols, self-reference SG)
  5. Cluster Placement Group: 所有节点必须在同一个 PG 里
  6. 所有节点必须在同一个 AZ

  7.3 Cluster Placement Group（CPG）

  作用： 让 EC2 实例在物理上尽量近（同一个 rack / spine），降低网络延迟。

  三种 Placement Group：

  ┌───────────┬────────────────┬─────────────────────────┐
  │   类型    │      策略      │          用途           │
  ├───────────┼────────────────┼─────────────────────────┤
  │ Cluster   │ 同 AZ 物理相邻 │ 多节点 GPU 训练（必需） │
  ├───────────┼────────────────┼─────────────────────────┤
  │ Spread    │ 分散到不同硬件 │ 高可用                  │
  ├───────────┼────────────────┼─────────────────────────┤
  │ Partition │ 分组，组内相邻 │ 大数据（HDFS）          │
  └───────────┴────────────────┴─────────────────────────┘

  Karpenter 配合：
  # EC2NodeClass
  metadataOptions:
    httpTokens: required
  userData: |
    # 放入指定 Cluster Placement Group
    [placement_group_name=training-pg]

  ---
  8. 完整架构图（AI 训练平台典型方案）

  ┌─────────────────────────────────────────────────────────────┐
  │                     EKS Cluster                              │
  │                                                               │
  │  Control Plane (AWS managed)                                 │
  │                                                               │
  │  ┌───────────────────────────────────────────────────────┐  │
  │  │                Karpenter                               │  │
  │  │   - Watches pending pods                              │  │
  │  │   - Provisions nodes just-in-time                     │  │
  │  │   - Mixes Spot + On-Demand                            │  │
  │  └───────────────────────────────────────────────────────┘  │
  │                                                               │
  │  NodePool: gpu-training        NodePool: gpu-inference       │
  │  ┌─────────────────────┐       ┌────────────────────────┐   │
  │  │ P4d/P5 Spot (70%)   │       │ G5 On-Demand (+Savings)│   │
  │  │ P4d/P5 On-Demand    │       │ G5 Spot (fallback)     │   │
  │  │ (fallback 30%)      │       │                        │   │
  │  │                     │       │                        │   │
  │  │ EFA + CPG           │       │ No EFA needed          │   │
  │  │ Same AZ             │       │ Multi-AZ OK            │   │
  │  └─────────────────────┘       └────────────────────────┘   │
  │                                                               │
  │  DaemonSets:                                                 │
  │  - NVIDIA Device Plugin (GPU exposure)                       │
  │  - AWS Node Termination Handler (Spot 中断)                  │
  │  - CloudWatch Agent (monitoring)                             │
  │  - Fluent Bit (log forwarding)                              │
  │                                                               │
  │  Batch Scheduler: Volcano / Kueue (gang scheduling)          │
  └─────────────────────────────────────────────────────────────┘

  Data Flow:
    S3 (dataset)  ←→  FSx for Lustre  ←→  GPU Pods
    Checkpoints:  GPU Pods  →  S3 (via mount or aws cli)

  ---
  9. 常见面试考点汇总

  ┌─────────────────────────────────┬───────────────────────────────────────────────────────────────────┐
  │              主题               │                              关键点                               │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ GPU 选型                        │ 训练用 P4d/P5（EFA 多节点），推理用 G5/Inf2                       │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Spot 策略                       │ 70% 节省是目标，依赖 checkpoint + interruption handler            │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Savings Plans vs Spot           │ 互补不互斥：Baseline 用 SP，Burst 用 Spot                         │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Karpenter vs Managed Node Group │ Karpenter = just-in-time + bin-packing + multi-instance，生产首选 │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ EFA                             │ 多节点训练必需；需要 CPG + 同 AZ + 支持的实例类型                 │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ GPU 共享                        │ MIG（硬件隔离）用于生产，time-slicing 用于开发                    │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Gang Scheduling                 │ 多节点训练必须用 Volcano/Kueue，否则半启动浪费 GPU                │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Cluster Placement Group         │ 所有训练节点必须在同一个 CPG + 同 AZ                              │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ Checkpoint-Resume               │ Spot 能用的前提，频率 15-30 分钟                                  │
  ├─────────────────────────────────┼───────────────────────────────────────────────────────────────────┤
  │ 节点 AMI                        │ EKS Optimized AMI with GPU，预装 CUDA + NVIDIA Driver + EFA       │
  └─────────────────────────────────┴───────────────────────────────────────────────────────────────────┘

  ---
  Module 1 Summary

  ┌─────────────────┬─────────────────────────────────────────────────────┐
  │     知识点      │                     一句话记忆                      │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ GPU 训练选型    │ P5 (H100) > P4d (A100) > Trainium；多节点必须 EFA   │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ GPU 推理选型    │ G5 (中小模型) / Inf2 (海量推理) / P4d (大模型)      │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ Spot 策略       │ 70% 省钱，前提是 checkpoint + 中断 handler + 多样化 │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ Savings Plans   │ Baseline 负载用，和 Spot 互补不互斥                 │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ Karpenter       │ 生产首选，just-in-time + bin-pack + Spot 多样化     │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ EFA             │ 多节点训练必需，需 CPG + 同 AZ                      │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ GPU 共享        │ MIG 硬件切分，生产推理提升利用率                    │
  ├─────────────────┼─────────────────────────────────────────────────────┤
  │ Gang Scheduling │ Volcano/Kueue，多节点 job 必需                      │
  └─────────────────┴─────────────────────────────────────────────────────┘
