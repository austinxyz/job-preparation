# Job applications and personal strategy — not for public view
positions/
raw_material/positions/
resumes/

# Daily prep notes — private, not for public view
daily/
/20[0-9][0-9]-[0-9][0-9]-[0-9][0-9].md

# Per-position job dashboards — private (HR correspondence, contact prep, etc.)
jobs/

# Local state
.claude/settings.local.json
.obsidian/workspace.json

# MCP server config — contains bearer tokens, do not commit
.mcp.json

# Skill asset trees — large binary/generated content, not for repo
.agents/
.claude/skills/ppt-master/
